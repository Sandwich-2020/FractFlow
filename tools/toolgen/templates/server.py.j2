"""
{{ tool_name }} Tool Server

This module provides direct MCP tool implementations for the {{ tool_name }} tool.
These functions are designed to be exposed as MCP tools and used by the AI server.
"""

import os
import sys
from typing import Dict, Any
from mcp.server.fastmcp import FastMCP

{{ import_section }}

# Initialize FastMCP server
mcp = FastMCP("{{ tool_name_snake }}")

{{ tool_definitions }}

if __name__ == "__main__":
    mcp.run(transport='stdio') 